enum FAQModels {
    // MARK: - Use Cases
    enum FAQData {
        // swiftlint:disable all
        struct Response {
            let faq: FAQType?
        }
          
        struct ViewModel {
            let faq: FAQType?
        }
        // swiftlint: enable all
    }
}
